let repeat = 0;

export function requestActions() {
    callBack: (el, arg) => {
        repeat = repeat + 1;
    }
    
    callDismiss: (el, arg) => {

    }

    callAwait: (el, arg) => {
        
    }
}