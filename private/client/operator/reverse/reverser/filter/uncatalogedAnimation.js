import macron from "../../../../../../dist/basics.js"

export function reverseUncatalogedAnimation(el, animName, arg) {
    macron('error', `Reversor de animação não catalogada em construção... Não o use por enquanto.`);
}